---
type: index
description: Active work with an end state. One folder per project, each with its own index.
---

# Projects

Active work that finishes: a build, a deal, a launch, a hire. One folder per project with its own `index.md`. When a project ends, move its folder to `archive/` and note the outcome on its index.

No projects filed yet.
